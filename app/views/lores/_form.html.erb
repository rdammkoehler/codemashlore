<%= form_for @lore, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :context, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :context, :class => 'text_area' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :ranking, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :ranking, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                lores_path, :class => 'btn' %>
  </div>
<% end %>
